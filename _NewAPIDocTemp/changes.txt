- update duktape library to version 2.6.0
- run all script instance startup routines on the same thread
- use javascript "strict mode"
- add default execution timeout of 500ms for instance startups/callback invocations
- replace socket overlapped IO with worker threads (fix Windows XP incompatibility)
- remove API.js, make all API functions native
- rewrite API documentation file
- add script.listen(inputListener) function for console input handling
- add script.keepalive(bKeepAlive) function to keep instance from automatically being destroyed
- add script.timeout(ms) function for timeout override

Changes which may break compatibility with old scripts:

- remove redundant `rom` interface because `mem` interface can access ROM
- remove redundant pc argument from events.onexec/onopcode/ongprvalue callbacks since there is `gpr.pc`
- change the `address` argument of `events.onread/onwrite` to more informative `CPUReadWriteEvent` object
- remove `GPR_ANY_ARG`, `GPR_ANY_TEMP`, `GPR_ANY_SAVE`

---------------------
TODO:
- script auto-run feature?
- finish implementing new server/socket
- finish implementing new events interface
- full test suite
- formatting/cleanup printfs, comments etc

- 'pj64' interface for high level control (savestate/pause etc.)
- mem.setblock
- mem.dramsize
- mem.saveaddress
- debug.showmemory
- debug.showcommands
- debug.getsymbol?
- debug.setsymbol?
- asm.encode
- asm.decode

Duktape.modSearch/require()
buffer console.log() so the message queue doesnt get flooded
fix input history / write input history to a file
make sure fs.readFile and mem.getblock return Buffer


